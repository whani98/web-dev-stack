<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.upload.mapper.BoardMapper">
<resultMap id="board" type="Board">
    <id column="no" property="no"/>
    <result column="title" property="title"/>
    <result column="content" property="content"/>
    <result column="url" property="url"/>
    <result column="create_at" property="createAt"/>
</resultMap>

<insert id="boardAdd" parameterType="Board">
INSERT INTO board(title, content, url) 
VALUES(#{title}, #{content}, #{url})
</insert>

<select id="boardAll" resultType="Board">
SELECT * FROM board
</select>

<select id="boardSearch" parameterType="int" resultType="Board">
SELECT * FROM board FROM WHERE no = #{no}
</select>

<update id="boardUpdate" parameterType="Board">
UPDATE board
SET title = #{title}, content=#{content}, url=#{url}
WHERE no = #{no}
</update>

<delete id="boardDelete" parameterType="int">
DELETE FROM board
WHERE no = #{no}
</delete>

<insert id="write" parameterType="BoardDTO">
INSERT INTO board(title, content, url) 
VALUES(#{title}, #{content}, #{file})
</insert>


</mapper>